<div class="container">
    <div class="row">
        <div class="col-md-10 col-md-offset-1">
            <%= @lights.each do |light| %>
                <%= light.name %>
            <% end %>
        </div>
    </div>
</div>